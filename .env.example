# === 必須設定 (Required Configuration) ===
# Supabase project URL - Get from Supabase dashboard
NEXT_PUBLIC_SUPABASE_URL=https://your-project-id.supabase.co

# Supabase anon key - Get from Supabase dashboard > Settings > API
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# === 自動設定 (Automatically Set) ===
# Node Environment (set by deployment platform)
NODE_ENV=development

# === オプション設定 (Optional Configuration) ===

# E2E Testing - Only needed for running Playwright tests
# E2E_MODE=false
# E2E_TEST_EMAIL=test@example.com
# E2E_TEST_PASSWORD=testpassword123

# Error Reporting - Recommended for production
# NEXT_PUBLIC_ERROR_REPORTING_ENABLED=false
# ERROR_REPORTING_API_KEY=your-error-reporting-key

# Analytics - Recommended for production insights
# NEXT_PUBLIC_ANALYTICS_ENABLED=false
# NEXT_PUBLIC_GOOGLE_ANALYTICS_ID=G-XXXXXXXXXX

# === セットアップ手順 (Setup Instructions) ===
# 1. ファイルをコピー: cp .env.example .env.local
# 2. Supabaseの設定値を入力
# 3. 必要に応じてオプション項目を設定
# 4. 設定チェック: npm run check:env

# === Vercel デプロイ設定 ===
# Vercel ダッシュボードで以下を設定:
# - NEXT_PUBLIC_SUPABASE_URL (必須)
# - NEXT_PUBLIC_SUPABASE_ANON_KEY (必須)
# - エラー追跡・分析ツール (本番推奨)
